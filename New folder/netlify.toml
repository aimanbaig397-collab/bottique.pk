# Netlify configuration for Andreas Boutiques
# https://docs.netlify.com/configure-builds/file-based-configuration/

[build]
  # Publish the current directory (where index.html lives)
  publish = "."
  # No build command needed — static HTML/CSS/JS
  command = ""

# Optional: redirects (e.g. force HTTPS is automatic on Netlify)
# [[redirects]]
#   from = "/*"
#   to = "/index.html"
#   status = 200
#   conditions = {Role = ["admin"]}
